#![allow(
    dead_code,
    reason = "the integration target imports the live eval runner to execute its pure contract tests"
)]

#[path = "../examples/lmstudio_router_eval.rs"]
mod lmstudio_router_eval;
